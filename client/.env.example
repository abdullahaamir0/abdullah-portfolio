# URL of the backend API (see /server). No trailing slash.
# Local development:
VITE_API_URL=http://localhost:5000

# Production example (after you deploy the server):
# VITE_API_URL=https://abdullah-portfolio-api.onrender.com
